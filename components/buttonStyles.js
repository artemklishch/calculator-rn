import { StyleSheet } from "react-native";

export default StyleSheet.create({
  btnTouch: {
    width: 70,
    height: 70,
    borderRadius: 35,
    justifyContent: 'center',
    alignItems: 'center',
  },
  btnText: {
    color: "#fff",
    fontSize: 24,
    fontWeight: 'bold',
  },
});
